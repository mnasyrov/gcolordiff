{
	"name": "Gmail Color Diff",
	"version": "0.1",
	"manifest_version": 2,

	"description": "The extension colorizes a diff patch inline in an email. Commonly, patches are sent by notifications from Git servers.",

	"permissions": [
		"https://mail.google.com/*"
	],

	"content_scripts": [{
		"matches": ["https://mail.google.com/*"],
		"js": ["gcolordiff.js"],
		"run-at": "document_end"
	}]
}
